# Progress

Working head of the project log. Update at the end of every working session —
even one-line entries. When stopping mid-task, write down where you are and
what's next.

**Session start**: read this file, run `scripts/status.py`, and diff the two —
the script prints the live numbers (Alembic head, entity/queue counts, matched
makes), so this file carries intent and decisions, not counters that go stale.

**Layering rule** (2026-07-29): this head stays small. Session Log keeps only
the last few entries; older entries, finished checklists, and completed review
findings roll into `docs/progress-archive/` verbatim and unedited. Decisions'
durable homes are the ADRs — log entries should be a claim plus a PR/ADR link,
not a restatement.

---

## Current Focus

Phase 1: **the first cars have identity, and their years + variants are landing.** ADR 0010 and **ADR 0011 accepted** (as-filed leaf models; series are lines, not entities; demo seed retired — every entity row is reconciler-produced). EPA vehicles.csv landed (49,995 per-variant rows, 1984–2027); the vPIC model-years backfill is COMPLETE (2,018/2,018 models have year lists). **1,735 `models` rows** under 129 companies (the 134 matched makes, minus double-MakeIds), `name` asserted through `field_provenance`; all 3 Santana slug collisions resolved as merges (ruled 2026-07-30: one page per wheelbase — 1,738 records reconcile onto 1,735 rows, model queue at zero); 280 model records wait under unmatched makes. 2,018 vPIC model records landed across all 247 makes; **134/247 makes matched** to 7,204 companies (post the v8 duplicate collapse); ADR 0009 implemented (`catalogue_periods` live, migration `76cb287dd71c`). 113 make-level `match_review` open (parked no-match pool), admission batch-triage proposals parked for the "barely cars" pass. Charter: **the focal point is the individual car's page.** The Wikidata models+generations sweep is landed and reconciled (ADR 0012 + 0013, both merged). Next: the year-pass/EPA ADR (part 6's quantitative bridge requirements, go pending), with the wd-model flag queues worked down alongside.

## In Flight

**The JLR brand-artifact merges are APPLIED** (2026-07-31, `ceec0f2` + `a914a1d`): Range Rover, Discovery, and Defender ruled Land Rover model lines, not brands — the filings agree (vPIC files all three under Make=LAND ROVER; EPA's 396 rows say make=Land Rover). Three `IDENTITY_MERGES` entries → Q35907, collapsed live (companies 7,204 → 7,201; standing rule: the merge script's dry-run list gets reviewed before every `--execute`), all passes converged, 131 tests green. The merge exposed and fixed two guard defects (see session log); the queue's one false cross-badge alarm is dissolved — **9 cross-badge flags remain, all real rebadges**. Ruled held as-is: Century (real 2025 Toyota spin-off), Scion (an additional-information plan exists), Ioniq (real short-lived division; empty catalogue is honest). Noted for the parked namesake/duplicate work: a second live "Audi" artifact (`audi-q136087723`, wins label-brand attribution for "Audi A4 B8" over Audi AG, saved by the same-family prefix rule) and the Jaguar Cars / Jaguar pair. **ADR 0014 ACCEPTED and IMPLEMENTED (branch `feat/adr-0014-year-pass-epa`, 2026-07-31; single PR carries the whole arc per the new one-branch rule)** — the year pass and the EPA attach, under the fundamentals-review doctrine: **the hierarchy is a goal per car**. The deliberation's AMG GT finding moved generation placement to the configuration (one 2019 model year holds C190 coupes beside X290 4-doors), so periods are pure time under models and `configurations.generation_id` is nullable, evidence-gated, NULL everywhere in v1. Live: **18,751 catalogue periods** (all 1,735 models have their year spine) and **23,523 configurations** from 42,920/49,995 EPA rows attached (exact 13,887 / baseModel 25,150 / word-boundary trim-parse 3,883), 13,565 1:1 `vehicle:` external ids, 121,496 configuration field assertions with provenance. Residue honest and queued: 495 unbridged-make rows + 6,580 no-model-match rows behind 802 distinct-string flags with candidates; `EPA_MAKE_MATCHES` registry starts empty, grown by resolving them. Both passes settle to exact no-ops; all six passes converge; 140 tests green; reconciler v11. (PR #28 merged 2026-07-31; ADR 0015 followed as PR #29, merged 2026-08-03 — reconciler v12.) Flag queues (wd-model + the new EPA queues): the next batch is deliberately deferred (2026-07-31). Parked: the namesake/ambiguous duplicate groups (cross-source checks first — his ruling), the Mazda chinesische-Automarke twin, TVR's plant twin (needs a deny decision), barely-cars (much later).

## Next (immediate)

1. **The queues ADR 0017 §4 opened** (implemented 2026-08-07, branch `feat/section-generations`): the `section_generation_review` articles (sections that neither reconcile to existing generations nor prove distinct — each resolution teaches the reconciliation ladder; **the 911 is now the marquee case**: 993/996 unlinked and 930 undated hold all 370 of its configurations honestly), 24 redirected nameplate articles (sitelink subjects moved), the section `implausible_value` production parses (incl. Prius XW10/XW20 per-sub-code lists and `celica-coupe-t200`, whose dating unlocks the whole Celica run). The `waits_undated_competitor` pool resolves mechanically as spans land. Alongside: the line-membership turn (453 line-case entities now representable), the Wikidata-adoption pass for when a section-born generation later grows its own entity (correspondence recorded per fetched Main target in the decision log since ADR 0018).
2. The flag queues, when the batch opens: wd-model (17 label-twin clusters via the curated model registry; 52 market/rebadge, 9 real cross-badges → negative pairs + future `vehicle_derivations` facts; 536 near-miss) + the EPA queues (~10 `unbridged_make` strings → `EPA_MAKE_MATCHES` entries; 802 `no_model_match` strings with candidates) + the `generation_overlap` residue (post-veto and post-demotion: boundary years — the 2006 MX-5 between NB and NC, the 911's 964/997/991/992 span overlaps — and rows with no body signal). Agent-assisted pre-screening of these queues (LLM proposes with evidence, human confirms, decisions land in registries as usual) is the requested direction (ruled 2026-08-07) — fold into the agent-review open question's ADR; the deterministic passes stay LLM-free.
5. ~~**The address system**~~ — **DONE** (ADR 0019, PR #37, merged 2026-08-11). Addresses are projections: `scripts/pipeline/recompute_addresses.py` re-derives them last in the fill sequence and converges to a no-op. Applied live. What the branch leaves owed, each recorded in the ADR: `/bmw/x5/2002/fwd` and `xDrive`-as-a-drivetrain both want vPIC's Body Class and option data (a source question, not a grammar one); the E350 casefold duplicate wants trim-case folding in the configuration natural key (entity resolution, and it recurs on every EPA landing); non-model-year period segments have no grammar because no such period exists yet; `models`/`model_lines` are not re-derived by the projection, and the vPIC models pass is now the one place a naming clash still withholds a row. Serving — 301s, disambiguation pages, freezing addresses at publication — belongs to the read surface.
6. **Thin read surface (F2) — unparked** (ruled 2026-08-07; the 2026-07-30 parking predates placed cars): now that ADR 0019 has landed, `v_configuration_full` + a thin FastAPI read layer + the first entity pages (company/model/generation/configuration), **plus a reviewer surface**: a queue browser rendering `reconciliation_flags` with candidates and car context. v1 reviewer surface is read-and-propose (verdict export → registry entries stay code-reviewed, per the resolutions-are-registries doctrine); direct-write review needs its own decision.
7. The matcher-evaluation methodology ADR (owed from the cleanup arc — before any precision number is quoted against the Tier-2/3 gate; now also owed the veto-overturn label shape from §4), the `confidence_score` write-or-drop decision. The "barely cars" pass — delayed until much later (ruled 2026-07-30), with the parked no-match pool.

## Next (Phase 1 horizon)

- ~~Year-level vPIC + EPA → the first configurations~~ — **DONE** (ADR 0014, 2026-07-31): 18,751 periods, 23,523 configurations. The remaining half is the generation-placement evidence pass (Next #2).
- ~~Wikidata models fetch~~ — DONE (ADR 0012/0013). Its 453 line-case generation entities wait for generation *time* (infobox spans or dated overlap), now decoupled from the year pass.
- Basic admin UI / review-queue interface (deferred until corroboration has shrunk the queues — ruled 2026-07-29).
- **Dagster adoption** (structure question raised 2026-07-30; timing refined 2026-07-31): the two-layer shape — logic importable in `carmanac/`, `scripts/` as thin entry shims — stays permanently; what Dagster replaces is the *conductor* (today: manual, in-session sequencing of the fetch→pass DAG). Timing ruled: the fill-and-corroborate phase stays manually conducted — Dagster earns its place when constantly-updating or newly-arriving sources appear (the forum/enthusiast tier, standing refreshes), not at the first write pass. Pipeline scripts (now `scripts/pipeline/`) become asset definitions calling the same library functions; the judgment scripts (`scripts/decisions/`) stay scripts forever — they are applied decisions, not pipeline.

Deferred review items (important, not blocking): confidence-score methodology (#6); `power_hp` rating standards (F8's second half, sibling ADR to 0009); CI running ruff + `alembic check`.

## Open Questions

These need decisions before they become blockers. Each should resolve to an ADR in `docs/decisions/` when settled.

- **Defunct/acquired makes**: Pontiac, Plymouth, Saab, etc. Are they top-level `makes` or do we model corporate parent relationships? (Leaning toward: makes stay top-level, add an optional `parent_company_id` self-reference.)
- ~~**Coachbuilders**~~ — RESOLVED by ADR 0005 (proposed). They are `builders`, not makes, and attach to vehicles via `vehicle_derivations` keyed on the *base generation*. The gray area dissolves under the WMI rule: a body house only raises the "is it a make?" question when it builds its own car, and then it earns a WMI and passes the normal test. Historical coachbuilding (Duesenberg/Murphy) is the common case — `derived_generation_id` NULL, so the car stays a Duesenberg.
- ~~**Builder product lines**~~ — RESOLVED at ADR 0005 acceptance (2026-07-27): a named product line (Singer DLS) *is* a model/generation under the builder company, linked to its donor via `vehicle_derivations.derived_generation_id`. No new entity kind.
- **Platforms** (NEW, from ADR 0005 §5): a future `platforms` entity — generations point at a platform, platforms carry `evolved_from` lineage (Urus → MLB Evo → MLB → VW Group; matches industry usage). Replaces the dropped `platform_shared` derivation type. Which platform a generation belongs to is a sourced claim and will conflict ("basically a Q5 underneath" vs. `platform: MLB Evo`) — normal reconciliation machinery applies. Wikidata P4243 is the obvious first source. Needs its own ADR when that ingestion is planned.
- ~~**Model-year spine vs. production periods** (F8)~~ — RESOLVED by ADR 0009 (accepted 2026-07-29): catalogue periods. Migration `76cb287dd71c` applied and merged (PR #17). The rating-standards half of F8 still needs its sibling ADR.
- ~~**vPIC external-id namespacing**~~ — RESOLVED 2026-07-29: kind-prefix both (`make:440` now, `model:<id>` from the models fetch's first run). Re-keyed live via `scripts/rekey_vpic_external_ids.py`; landers write prefixed ids.
- ~~**What a vPIC ModelId actually is**~~ — RESOLVED: **ADR 0011 drafted** (2026-07-30, from the two-part ruling). Models are as-filed leaf designations, each with its own page ("I want a 308" → "GTB or GTS?"); the schema absorbs per-make granularity (generations carry time, configurations carry engine/body/trim). **Series/lines are NOT entities** (second ruling): "3 Series" is an aggregation over models, served later by a source-asserted membership relation rendered as a view — no source's series entity may mint a `models` row, and the seed `3-series` chain is retired (live, `scripts/retire_demo_seed.py`). One interpretive carve-out awaiting confirmation in the ADR: **Mercedes `C-Class` stays** — it is the *filing*, the finest grain vPIC holds for that make; removing it would orphan Mercedes' catalogue and the next pass would lawfully recreate it. Near-duplicate filings (`228`/`228i`, `308GTS`/`308 Convertible`, `Boxster`/`718 Boxster`) wait for model-level curated merges (ADR 0011 §5).
- **What a catalogue period hangs under when no source asserts a generation** (2026-07-29 — **blocks the year pass**, same shape for EPA; NARROWED 2026-07-30): `catalogue_periods.generation_id` is NOT NULL, but neither vPIC nor EPA has a generation concept, and fabricating one per model is the provenance smell ADR 0009 rejected one dimension over. Direction confirmed: wait for a generation-asserting source. That source is **Wikidata, with eyes open** — probed live 2026-07-30: generation entities exist and chain (E30 Q838837, P155/P156 → its E21/E36 neighbours), but only some carry P179 to a series (E30 does, its neighbours don't); **labels don't separate levels** (Q466066 the series, Q730915/Q608824 the neighbour generations, and the seed's Q194352 are ALL labelled "BMW 3 Series"); **neither does P31** (E30 is plain `car model`, same class as a nameplate). Level determination must be structural — P179 membership, follows/followed-by chain position, dates — never label or class. Coverage is uneven (no 4Runner generation entities via the standard relations). The sweep must discover multi-property and expect gaps. Two bridge modes onto as-filed models: direct match (4Runner ↔ 4Runner, M3 ↔ M3) and series-membership inference for badge filings (330i ∈ 3 Series + year overlap → E46). The QID fan-out wrinkle is dissolved by ADR 0011 §4: external ids are written only on one-to-one correspondence — a source entity mapping to none-or-many of our rows gets no `external_ids` row, its facts land with raw-record provenance, and identity resolves by natural key. Source choice is deliberately **not Wikidata-only** (ruled 2026-07-30): Wikipedia's per-generation infoboxes (Tier 2) look stronger than Wikidata's patchy structure and get evaluated beside it; Wikidata stays first for cheap structured reach. Makes no source covers: their years stay raw-only. Lands with the Wikidata models ADR.
- **Company/brand duplicates beyond the match queue**: the 2026-07-29 merges resolved the pairs vPIC exposed, but the earlier review counted ~121 duplicated names overall (Mazda ×3 shape). The rest surface as sources touch them; a systematic sweep of exact-name duplicate companies is possible any time via one query + the same merge machinery.
- **Spec rating standards** (F8): `power_hp` is standardless (SAE net / DIN / JIS gross indistinguishable) and only EPA cycles get first-class columns. Rating-standard/test-cycle lookups, with non-EPA figures in EAV per the 80% rule. Same ADR as above, or its sibling.
- **Concept cars and prototypes**: in scope or out? (Leaning toward: separate boolean flag on `configurations`, default to production-only in queries.)
- **Race-only configurations** (GT3, Group B, etc.): in scope? (Leaning toward: yes, with a flag.)
- ~~**Slug strategy**~~ — RESOLVED by ADR 0019 (2026-08-11), and not the way the 2026-07-29 note leaned. Neither stable slugs nor slug+id: an address is a **projection**, recomputed from current data every run, and nothing may key on one. The source-neutral ruling stands and is now enforced by construction — no mechanical disambiguator exists at all, so no source's ID scheme can reach a public address. Historical addresses are deliberately NOT kept: nothing is published, and a redirect map belongs to the read surface at publication.
- **Corroboration-driven queue reduction** (ruled 2026-07-29, a design requirement for the vPIC pass): presence in an authoritative source is inherent authentication — a vPIC WMI/make match should auto-resolve that entity's `admission_review` flag rather than wait for a human. The review queue shrinks from evidence first, manual review second. The **review-queue interface is deliberately deferred** until multi-source corroboration exists, so reviews aren't bare is-this-a-car-company judgments.
- **Agent-assisted review** (raised 2026-07-29, future): once resolved flags accumulate as a labeled set, an agent trained/grounded on the confirmed data could pre-screen the review queue. Sequencing: after vPIC corroboration and real queue-working experience.
- **Company eras and revival** (from the Bugatti merge, 2026-07-29): a single `defunct_year` cannot represent multi-era companies — Bugatti (Molsheim d.1963 → EB110 era → VW/Rimac era, alive), Scout Motors (revived 2022), MG (Morris → BL → MG Rover → SAIC), **Santana Motor** (ruled 2026-07-30: Spanish company, Land Rover licensee era, defunct 2011, now being revived around EVs — both its vPIC MakeIds stay on the one company row per the Bugatti rule, whichever era files next). Company pages should be able to show eras; "defunct" must not mean permanently dead. Needs an ADR (era/active-period modeling vs. prose); until then the Bugatti canonical-record projection (`defunct 1963`) stands as a known-wrong tentative value.
- **Multi-language attribute names**: do we store one canonical English attribute key and translate at the frontend, or store localized labels in `attribute_definitions`? (Leaning toward: canonical English keys, localized labels as a separate concern later.)
- **Reference DDL vs. models as source of schema intent** — RESOLVED in practice: the SQLAlchemy models are now the source of truth. `docs/schema_phase1.sql` and `docs/schema.md` are flagged partially-superseded by ADR 0002/0003; full rewrite (or generating the DDL from models) is a tracked follow-up.

## Resolved Decisions

(ADRs go in `docs/decisions/` — this is a quick index.)

- [0001](docs/decisions/0001-leaf-entity-naming.md) — Leaf entity named `configurations`, not `variants`. Accepted 2026-06-15.
- [0002](docs/decisions/0002-entity-fact-split-and-field-provenance.md) — Entity/fact split; field-level provenance in `field_provenance`. Accepted 2026-07-22.
- [0003](docs/decisions/0003-raw-landing-zone-and-external-ids.md) — `raw_scrape.raw_records` landing zone; `external_ids` mapping replaces `wikidata_qid` columns. Accepted 2026-07-22.
- [0004](docs/decisions/0004-raw-record-retention.md) — Raw record retention tiered by re-fetchability: Tier 3/4 archival, Tier 1/2 prunable cache, bug artifacts always deletable, distrust never justifies deletion. Amends the CLAUDE.md invariant. Accepted 2026-07-24.
- [0005](docs/decisions/0005-what-counts-as-a-make.md) — What counts as a make: manufacturer responsibility (issues its own VINs), **no exceptions**; under ADR 0006 the test classifies rather than admits. Derivation is one `vehicle_derivations` fact table keyed on the base generation; the nullable derived side records **catalogue placement** (own entry under the builder vs. stays under the base make), decoupled from the VIN test — legal status lives only in `company_role_assignments`. `platform_shared` dropped in favour of a future `platforms` entity. Accepted 2026-07-27 as amended; implemented in `05e766a04a5f`.
- [0006](docs/decisions/0006-companies-not-makes.md) — One `companies` table; "make" becomes a role. Alpina is both a manufacturer and a builder, so two tables would give one company two rows and two pages. Accepted 2026-07-24, implemented in `5cbf6be81036`.
- [0007](docs/decisions/0007-reconciler-policy-and-first-pass.md) — Reconciler: deterministic raw→assertions→projection pipeline; **one engine + one thin mapper per source**; QID-exact identity only in v1 (no fuzzy auto-merge until a labeled set exists); **strict admission, branching outwards** — vetted classes admit, deny-listed exclude, unknowns *quarantine* with `admission_review` flags (under-admission is the cheap error); `manufacturer` role asserted from both Wikidata classes (Pontiac counts; vPIC arbitrates later); tier → affinity → recency → flag; new `reconciled_records` + `reconciliation_flags` tables and `companies.website`. Accepted 2026-07-27; implemented 2026-07-28.
- [0008](docs/decisions/0008-vpic-matching-and-corroboration.md) — vPIC matching + corroboration: match ladder (curated registry → unique exact-normalized-name → `match_review` flag with candidates, never fuzzy-auto); a confirmed match writes `external_ids`, asserts the `manufacturer` role from vPIC, resolves admission flags as `corroborated_by_vpic`, and admits matched quarantined entities (the fourth way in). Corroboration is US-scoped until more sources land; the corroboration trail is the future confidence methodology's raw material. Accepted 2026-07-29; implemented same day.
- [0009](docs/decisions/0009-catalogue-period-spine.md) — The 4th level is a **catalogue period**: `model_year` (start = end; what vPIC/EPA assert), `production_period` (Euro/JDM "built 1998–2005"), or `phase` (zenki/kouki, Phase 1/2), in one table renamed `catalogue_periods`. Five-level hierarchy and one-page-per-configuration unchanged; US per-year pages unchanged; fabrication in either direction rejected on provenance grounds; same-kind overlap flags, never constrains; mixed granularity resolves by containment. Review notes: aggregation above stays (the generation page is the shared page); C190/R190-style code families are one generation's `chassis_codes`. Accepted 2026-07-29; implemented in `76cb287dd71c`.
- [0010](docs/decisions/0010-vpic-models-pass.md) — The vPIC models pass: nameplates under **matched makes only** (an open make question must not fan out into model-shaped copies); identity ladder `model:<ModelId>` then create under the resolved company; **slug collisions flag rather than auto-suffix** (two same-slug models under one make are usually the same nameplate, and `gt-r-2` would mint the duplicate identity the merges just cleaned); `models.name` projects via `field_provenance` so Wikidata can arbitrate later; nameplate level only — no generations, periods, roles, or derivations. Accepted 2026-07-29 as written; implemented same day (1,735 rows, reconciler v7).

- [0019](docs/decisions/0019-the-address-system.md) — **Slugs are addresses, not identity.** Nothing joins on a slug, no registry keys on one, and no row fails to exist because its address is taken: every slug column is nullable and unaddressed rows *are* the review queue. An address is a projection recomputed by `recompute_addresses` every run. The route map becomes the hierarchy (`/<company>/<model>/<year>/<car>`, generations at `/<company>/generations/<code>`), so a car's address is only the tail and is unique within its model year, and a generation wears its bare chassis code unless another generation under that company shares it. Contested company names go to evidence — a filing, then nameplates — never arrival order. Accepted 2026-08-11 (PR #37); migrations `f3c81a4d6b27`, `c5d90b3e7a41`.

## Known Risks / Things to Watch

- **Scraping ToS exposure**: avoid commercial sites without clearly public data. Lead with Wikidata + government APIs to minimize risk while volume is small.
- **Wikidata coverage gaps**: strong for mainstream Western and Japanese makes, weaker for Soviet-era, Chinese pre-2010, Indian, and Brazilian domestic-market vehicles. Tier 3 sources will be required earlier than expected for those.
- **Wikidata class modelling is not a clean taxonomy.** `automobile manufacturer` and `car brand` overlap inconsistently — Pontiac is a brand but not a manufacturer; Saab is both. Assume any single class misses real marques, and re-check coverage against a known list whenever the query changes. The landed 7,223 also include plants and subsidiaries ("KINTO Europe"), so **what counts as a `make` is an unresolved reconciliation question**, not a solved one.
- **EAV query performance** at scale (>500k configurations × N attributes). Plan to benchmark with synthetic data before declaring schema final.
- **Entity resolution debt**: every source added without a solid matcher compounds the reconciliation problem. Do not add Tier 2/3 sources until matcher precision is measured on a labeled set.
- **Schema intent duplicated** between `docs/schema_phase1.sql` and the SQLAlchemy models. Mitigated 2026-07-22: models declared the source of truth, DDL/schema.md flagged stale. Full rewrite still owed.
- **`confidence` has no methodology yet** (review #6). It is written on facts but means nothing until defined (e.g. tier weight × match confidence). Do not let downstream logic weight it before then.

## Session Log

End-of-session notes, newest at top. Last few entries only — older ones live
in [docs/progress-archive/](docs/progress-archive/2026-06--07.md), along with
the completed F1-F9 fix queue and the 2026-07 review findings.

### 2026-08-11 (part 2 — the route map becomes the hierarchy; the recompute runs live)

- **The scheme, ruled and shipped**: `/<company>/<model>/<year>/<car>`, with generations at `/<company>/generations/<code>`. Each segment adds only what the ones before it do not say, so truncation is a feature — `/bmw/m3/2004` is that year's M3s, `/bmw/m3` is every M3. The `/makes/` and `/cars/` prefixes are gone and company slugs sit at the root, so root literals are reserved in `RESERVED_COMPANY_SLUGS` (zero of 7,201 live slugs collide, a pure guard).
- **A car's address is the tail only** — trim, then drivetrain when the trim does not already say it. That made global uniqueness impossible rather than merely unnecessary (23,523 cars compose 2,588 distinct tails; `fwd` fits 3,161), so migration `c5d90b3e7a41` moves the constraint to `(catalogue_period_id, slug)` — what the URL actually promises. A car nothing distinguishes wears `base`, so a year page is always an index. The de-stutter un-suppresses itself where suppressing would merge two cars in one year (6 of 10,873 model years), leaving 23,511 addresses dependent on nothing but their own row. **This also bought the real prize: a company rename now moves one address instead of 23,523.**
- **A generation wears its bare chassis code** (`/porsche/generations/964` — what people call it, and the nameplate is already in the path); 253 switched. Two data-driven exceptions keep the composed form: a code shared by two generations under one company (Celica/Supra A60, Camry/Camry Solara XV20 — nine pairs, real platform sharing) and a generation carrying several codes, where taking the first would put array order in a URL.
- **Ran live, and it caught what the dry runs could not.** status.py showed assertions up 1,369 after a pure address recompute: placement provenance recorded its observed value as the generation's *slug*, so re-addressing superseded every assertion citing one — an address rewriting permanent history, and it would have recurred on every grammar change. Now `generation:419[1989–1994]`, keyed on the row. Flag and decision `detail` still render slug pairs deliberately: recomputed every run, never a key, and readable is what a reviewer needs. **The line: provenance and identity never cite an address; display may.**
- **Verification**: all nine passes re-run with zero creation, withdrawal or new flags; recompute run 2 an exact no-op; 193 tests green; `alembic check` clean; status.py before/after differs only by the placement restatement and the newly-honest unaddressed counts (122 companies, 5 generations, 1 car). Two adversarial reviews rode the branch — the first found the alias scaffolding's index-usability and TRUNCATE holes, the second found the registry key silently pointing at a QID namespace for 477 models, `/makes/audi` going to a two-statement stub, and addresses being taken before released.

### 2026-08-11 (part 1 — ADR 0019 rewritten: addresses are projections, and the scaffolding goes)

- **The review of the branch inverted its premise.** ADR 0019 as written treated an address as a commitment and built to protect it: `slug_aliases`, 22 triggers, an advisory lock, alias-union occupancy in five mint sites, abort-on-stale-registry-key, and two dry-run-gated rename scripts. Ruled 2026-08-11: **slugs are page addresses, not data** — nothing joins on them, the front end they serve does not exist, and the machinery was overhead for a promise nobody had made. The correct question was never how to protect a slug but why anything depended on one.
- **The dependency was an accident, and dated.** Three `policy.py` registries keyed recorded human judgments on slug pairs (`toyota/highlander`), so a rename silently disarmed them — for the negative registry, back into the exact match a human rejected. Git says why: the slug question was parked as an Open Question on 2026-07-29 ("low urgency, no pages exist") and `WIKIDATA_MODEL_MATCHES` landed two days later keyed on it. The charter's join-key invariant governs entity↔source joins and never covered code↔row pointers; the gap is now closed with an explicit invariant (**slugs are display addresses, not identity; a row must be able to exist with no slug**).
- **The rewrite**: one `carmanac/reconcile/addressing.py` owns the grammar and `recompute_addresses` re-derives every address from current data, converging to a no-op like any other pass. Deleted: the alias table and its migration, all 22 triggers, the advisory lock, the stale-key abort, both rename scripts (467 lines — one carried a function whose docstring admitted it *"Mirrors `_EpaAttachPass._slug`"*), and 385 lines of tests for the machinery. The three registries key on the model's own vPIC id; `model_lines` resolve by normalized name; `normalize_name` moved to `names.py` to break the resulting import cycle. Migration `f3c81a4d6b27` makes every slug column nullable, so a contested namesake or an unnameable generation exists and simply has no address — **unaddressed rows are the queue**, which is what status.py now counts.
- **Contested addresses are decided by evidence, not arrival order** — the census that forced it: of 100 contested company clusters, only 6 contain a company with any cars, and 3 of those 6 are contested by two-statement German-only Wikidata stubs ("chinesische Automarke" — Toyota, Honda, Mazda). Vacating the bare name symmetrically would have taken `/makes/toyota` from the company with 54 nameplates and a vPIC filing. A filing plus nameplates wins; ties go to the incumbent; the loser waits for a pin.
- **Verification**: all nine passes re-run as exact no-ops (`lines: matched=245` confirms the name-keyed lookup mints no duplicates; `unrouted=0` confirms the re-keyed routing resolves; EPA `created=0 existing=23523 flags=0`); the address recompute converges on run 2 with every counter zero; **190 tests green**; `alembic check` clean; branch net now +965/−111 against main, down from +1,815. Nothing has been recomputed on the live database — the dry-run diff is captured for the standing gate.

### 2026-08-10 (ADR 0019 written and implemented: the address system)

- **ADR 0019 drafted** (Proposed; acceptance at PR review) after a verified live census and a three-proposal/six-critique design pass. The six decisions: (1) `slug_aliases` + database triggers — a rename without its alias row is mechanically impossible for every writer including raw psql, freed addresses can never be re-minted (BEFORE INSERT guard), and a row cannot delete without forwarding its address (merge discipline; `carmanac.allow_address_drop` is the ADR 0004 own-artifacts escape); (2) company collisions flag-never-suffix with curated `COMPANY_SLUG_OVERRIDES` pins — the QID suffix and the non-ASCII external-id fallback both die as the same source-leak; contested clusters vacate their bare slug into `RESERVED_COMPANY_SLUGS`; (3) slug-pair registry keys migrate atomically with renames, enforced twice — pass init aborts on any stale key (negatives fail closed) and the rename scripts refuse to execute ahead of the rewrite; (4) the canonical generation address is company-anchored (`/makes/<c>/generations/<slug>`, flagged charter revision), model-nested forms stay valid but non-durable, and mint-time drift guards (year-range/`category-` slugs flag, nameplates strip at both sites) end the grammar drift; (5) the leaf is `/cars/<slug>`, composed slugs ratified and completed (body/market tokens by fact, US omission a frozen constant — the whole fleet stays conformant), the silent `-2` counter dies; (6) new kinds join by registration — kind, scope, grammar, collision policy, reserved segment.
- **Implemented whole on the branch** (reconciler v16, migration `e8f2a91c7d30`): the alias table + 21 triggers + advisory-lock serialization of address writers, alias-union occupancy in all five mint sites (the model-line natural-key lookup resolves through aliases — the one rename-fights-a-pass hazard, closed), loud registry validation in both consuming passes + a status.py registry/address health section, the v16 mint changes, and the two dry-run-gated scripts in `scripts/decisions/`. 20 new tests (200 green): the rename-cannot-drop-address contract is pinned through ORM and raw SQL both.
- **Dry-runs captured for the standing gate** (nothing executed): the company batch computes 19 suffix drops (`tesla`, `techrules` among them), auto-holds the AUDI twin (its bare base is claimed by Audi AG's vPIC make name — merges outrank renames), finds 100 contested bases wanting reservation and ~220 cluster members wanting pins, and enumerates 1,206 leaf recomputes — the 179 Tesla children, the 38 double-hyphen malformations, and the shared-slugify hyphenation class (`aspec` → `a-spec`, `types` → `type-s`); the one `-2` counter row is correctly kept as the flagged casefold duplicate. The generation batch renames 2 (`category-honda-hr-v-1st-generation` → `hr-v-first-generation`, `civic-11th-generation` → `civic-eleventh-generation`) and honestly waits on `2000-2007-subaru-impreza` (no codes, no ordinal on record).
- **Reviewed adversarially before the PR** (3 dimensions x find-then-verify): 9 confirmed findings, all fixed on the branch, 6 refuted with reasoning. The three that mattered: `TRUNCATE <address table> CASCADE` silently emptied `slug_aliases` (row triggers do not fire on TRUNCATE) — now a statement-level guard on the alias table itself; the conformance guard's year-range test was prefix-anchored, so the four live `de-ville-1961-64` rows were classified conforming — the test now matches a range anywhere and the batch shares the same function, so mint and sweep cannot disagree; and `merge_duplicate_companies.py` had never been taught the merge sequence, so the next curated merge would have aborted on the delete guard — it now forwards the loser's address, re-points and re-scopes aliases, and takes the address lock. Also fixed: the guards' `IS NOT DISTINCT FROM` scope predicate was not index-usable (a measured O(aliases x rows) cliff — 48s for a fleet re-land at 50k aliases, 0.2s after the split), the rename trigger's `ON CONFLICT` could silently relabel a hand-written merge alias (removed — it raises), and the compound-nameplate stripping that turned `Mazda3` into `3` (stripping now requires the remainder to contain a letter, so the 12 names read `Mazda3 (BK)` / `Polestar 2 (P317)` again).
- **Verification**: all nine passes converge — run 2 an exact no-op; run 1's only churn is the 12 one-time name corrections, which the stripping fix restored to their sane forms (slugs never touched). status.py before/after differs only by the alembic head and those 12 assertions; registry/address health reads ok; **204 tests green**; `alembic check` clean; the trigger's dynamic SQL — the one path no test reaches, unvalidated by Postgres until runtime — exercised by hand.

### 2026-08-07 (part 3 — ADR 0018 implemented: wrong-grain demotion + the Main-target fetch)

- **ADR 0018 accepted and implemented** (branch `chore/generation-inventory-hygiene`, one PR carries the arc; reconciler v15, no schema change). **Half A — the demotion is a registry + script pair**: `NOT_A_GENERATION` (QID → verdict) gates the wd-models pass (`held_not_a_generation`, no link assertion or refresh) and the sections pass's competitor sets — the gate is what keeps the retirement from resurrecting on re-run; `scripts/decisions/demote_non_generations.py` retires links by self-supersession behind the standing dry-run gate. Ruled at the dry-run review: GT2 `trim_lineage`, Targa `body_style`, Celica GT-Four `trim_lineage` (the nameplate article carries seven real generations), Civic Hybrid `powertrain_lineage`; the Civic Type R FL5 ruled the opposite way — a real generation, FL5 is its code — and the 5 Turbo stays the unruled homologation gray case. Executed: 4 links retired, zero placements cited any of the rows before or after, wd-models re-ran twice with zero resurrection. Rows, external ids and facts all stay (trim-line/derivation modeling is a future ADR).
- **The demotion un-blocked the Celica**: with `celica-gt-four` out of the competitor set, the nameplate article minted its whole seven-generation run (six dated 1970–2006; `celica-coupe-t200` flags its production and waits, holding the model's placements honestly). Overlap queue 480 → 378: the GT2-driven 213 are gone (dismissed `candidates_no_longer_overlap` or re-decided as `waits_undated_competitor`); the 911's 100 remaining are real span overlaps among 964/997/991/992, and it places nothing yet for the honest reason — 930 undated, 993/996 unlinked (the `section_generation_review` queue's job).
- **Half B — the Main-target fetch**: 16 `{{Main}}` targets of undated minted sections landed as `section-main:<QID>#<ordinal>` records (kind readable from the id); the sections pass and placement's decision-time loaders read them behind two grain guards — redirected targets assert nothing, and so do bare-title targets (the pre-implementation probe caught Kia Sephia: a whole-nameplate 1992–2003 span that parses cleanly — the Civic-redirect lesson arriving without a redirect). Dash templates (`{{nbndash}}` et al.) normalize before span parsing. **13 of the 17 Main-pointer generations dated** with per-field provenance to their target records: all four MX-5s (35/36 configurations placed; the 2006 car is an honest NB/NC boundary flag), Prius XW30/XW50 (XW10/XW20 flag multi-range per-sub-code lists), both R8s, all three X1s, the Leaf. The fragment target (`q60-v36` → `Infiniti G Line#G37`) and the Spectra's nameplate deferrals held correctly. Sweep-QID correspondence per target recorded in the decision detail for the future adoption pass.
- **Placements 2,389 → 2,652 with zero withdrawals**; spans 355 → 373 of 531 generations; all nine passes converge to exact no-ops twice; **180 tests green** (9 new); status.py before/after diffed clean and now prints the model coverage funnel (1,114 with configurations → 398 QID-attached/routed → 363 articled → 147 linked → 85 placed) so placement coverage always shows its denominator.

### 2026-08-07 (part 2 — PR #35 merged; the branch reviewed whole; PROGRESS goes neutral)

- **PR #35 merged** after one CI fix (a `ruff format` miss on the placement pass — the formatter now runs beside the linter in the pre-push habit). The branch-review page (findings, the Miata counter-example, the two-lens pitfall list) published as an artifact; its censuses landed in Next: the wrong-grain demotion batch (ruled: GT2 and Targa are not generations; six-row species, dry-run gate), the Main-target fetch (17 undated section generations incl. all four MX-5s dateable from Wikipedia itself), and the agent-assisted queue pre-screening direction (LLM proposes with evidence, human confirms; deterministic passes stay LLM-free).
- **This log now speaks neutrally too** (ruled 2026-08-07): the author-voice rule's PROGRESS carve-out is closed; existing entries swept, archives stay verbatim per the layering rule.

### 2026-08-07 (ADR 0017 §4 implemented: section-minted generations, the body veto, the proof car placed)

- **Probe first, as ruled** (108 of the 432 nameplate pages, every 4th by QID, plus the AMG GT pair): 43% carry generation sections; ordinals near-universal (99%, extended table to thirtieth for Crown's "Sixteenth"); heading codes follow a positional `(CODES; YEAR)` convention the strict label extractor missed (letters-only GD/GE/NA legal by position); 77% of sections carry their own infobox; `{{Main}}` pointers on only 18% — too thin to carry dedup alone; the 3 "ordinal collisions" were all benign `Nth generation models` subsections. Keying settled as `section:<QID>#<ordinal>`; codes and heading text are facts, never identity; heading years never become spans (a fabricated open end would contain everything). All recorded in the ADR §4.
- **Two rules earned by the probe**: (1) *all-or-nothing per article* — a partially-minted article is the link-completeness trap generalized (an unlinked competitor the undated guard can't see), so duplicate/non-contiguous ordinals, slug collisions, or unreconcilable sections flag `section_generation_review` (new kind, migration `b7d1a2c4e9f0`) and mint nothing; (2) *labeled subranges defer to a single unlabeled range* (§2 amendment) — the C190's `production` lists its run bare beside Black/Track Series lines; per-body and per-plant lists label every line and keep flagging. The amendment retro-dismissed 9 open parse flags and dated 3 more generations.
- **The live run** (all landed, reconciler v14): 434 articles fetched (432 mechanical + 2 curated routings), **374 generations minted from 121 articles** (151 → 525 total; spans 42 → 355; links 151 → 552), 27 sections reconciled onto Wikidata-born generations (corroborating links), 28 articles flagged, 24 redirects held. **Placements 197 → 2,389**, every one with field-level provenance to the deciding article record; 432 rows used the body veto (EPA `VClass` two-seater class + `pv/lv` door-volume fills from raw, trim words as fallback; wagon/SUV classes deliberately mute — EPA reclassed the 2025 GT 4-Door as a wagon). Waits stay honest: 19,449 no-dated-generation, 1,205 undated-competitor, 480 overlap flags (boundary years and signal-less rows only).
- **The proof car is placed, exactly as ruled**: `SECTION_ARTICLE_MODELS` routes both AMG GT articles onto the one vPIC filing (the curated "link means this catalogue contains these cars" judgment — the 4-Door page turned out to be its own two-section nameplate article, so X290/C590 mint mechanically, nothing by hand). All 66 configurations place — coupes → C190/R190, 4-doors → X290, 2024+ coupes → C192 — **zero overlap flags on the model**; the 2019 S Coupe's decision reads `model_years_exact` with `body_vetoed: [amg-gt-x290, amg-gt-c590]`. All eight passes converge to exact no-ops twice; **171 tests green** (13 new); status.py before/after diff clean.

### 2026-08-06 (part 2 — the ADR splits under review: 0016 structure, 0017 evidence)

- **The review question — "why trust Wikidata for generations over Wikipedia?" — held up, and reshaped the documents.** The probe: Wikidata's generation entities are sitelink shadows of Wikipedia's per-generation *articles*; where Wikipedia keeps generations as *sections* of the nameplate page (the review's Mercedes-AMG GT link: `#First_generation_(C190/R190)`), Wikidata has nothing — which is why the proof car had no candidates at all. Funnel measured: 1,114 models with configurations → 432 with reachable nameplate articles → **51 with any linked generation**. A 36-article sample of the 381 articled-but-generation-less models: ~1/3 carry per-generation section infoboxes, headings in a cross-marque convention (`First generation (XW10; 1997)` — ordinal, code, year, plus maintained anchor spans).
- **ADR 0016 rewritten to the pure structure decision** (company-anchored generations + `generation_model_links`; accepted, verified) and **ADR 0017 split out for evidence**: §1–3 the implemented machinery (sitelink fetch, time-and-codes with precedence, dated-overlap placement with both live-run amendments) accepted; **§4 Proposed** — section-minted generations with Wikidata demoted from gatekeeper to contributor (an explicit revision of ADR 0012's "only P179 creates"), keying/dedup/naming questions laid out for review. Verdict on "is 0016 doing what it intended": structure yes (links adopted, line-case unblocked, no-ops hold); evidence correct-but-starved — the choke point is existence, which is §4's job.
- **Session close: ADR 0017 approved as written** — §1–3 accepted-and-implemented, §4 approved in principle ("unsure how it will turn out" → the implementation branch proves it out, probe-first). PR #34 merged; §4 implementation gets its own branch. Third-person attributions swept from all ADRs and docstrings (the author-voice rule extended explicitly to decision records).
- **The second ruling — the C190/X290 are different cars, and the overlap flag was never the goal**: the conflation is vPIC's one-Model filing (Mercedes' naming), not the sources' — Wikipedia and Wikidata keep the sports car and the 4-Door fully separate (own articles, own entities). Folded into ADR 0017: **body evidence is a candidate VETO, not an overlap tiebreaker** (a 4-door is never a C190 candidate; EPA's unconsumed ~100%-fill size class is the strong signal — `body_style_id` is 0-filled today), and the §4 link-completeness trap named: with only C190 linked, 2019 4-doors would *uniquely* year-match it and the undated-competitor guard can't fire against an unlinked car. End state: coupes → C190, 4-doors → X290 (its own generation from its own article, link curated, never fabricated by section parsing); `generation_overlap` remains only for rows with no body signal.

### 2026-08-06 (ADR 0016→0017: generations restructured, Wikipedia lands, the first placements)

- **ADR 0016 accepted and implemented** (branch `feat/generation-placement`, one PR carries the arc). Session-start probe set the design: Wikidata holds almost no generation *time* (936/14,524 records with any start date; even the E30 has none) but 8,131 sitelinks point at English Wikipedia, whose infoboxes carry `production` AND `model_years` (the E30 live probe). The structural ruling mid-review: **generations are company-anchored index entities, not children of one model** (one E46 covers 325i/330i/M3) — a flagged invariant revision. `generations.model_id` → `company_id` (migration `c113fff36784`, refuse-don't-discard both ways); which models a generation spans is now `generation_model_links`, a `model_line_members`-shaped fact table seeded sourceless from the retiring FKs and adopted by the wd-models pass with full provenance (151/151 on run 1, run 2 no-op). Reconciler v13.
- **Wikipedia enters via sitelinks, not matching** (Tier 2; the matcher gate is explicitly not crossed — identity inherited from the QID). New `carmanac/ingest/wikipedia/` lander: 446 articles fetched (all landed, zero dead links). The infobox pass asserts production spans + title-parenthetical chassis codes with the precedence ruling (infobox supersedes label-derived; the wd-models pass keeps asserting but skips projecting outranked fields — `assert_field_facts` extracted to the engine with `skip_projection` to give the ruling teeth). Parser tuned against the real corpus: 280/375 production spans parse; 74 multi-range fields flag honestly.
- **The first 197 placements exist** — the Camry's whole eight-generation run (via `model_years` exact), Legacy's seven (production + the accepted end+1 slack), Fit, G45. 354 `generation_overlap` flags (real boundary-year ambiguity), 22,356 wait honestly. **The first live run taught two rules, both now in the ADR as amendments with regression tests**: (1) *undated competitors bar placement* — a 1991 Celica convertible placed into `celica-gt-four` because the GT-Four page was the only dated candidate; 616 configurations now wait in `waits_undated_competitor` instead; (2) *redirected articles assert nothing* — the "Honda Civic Hybrid" sitelink resolves to the whole-nameplate Civic page, whose 1972–present span landed on one generation; 7 redirects caught, bad spans tombstone-healed. 650 wrong-or-premature placements withdrawn with their full supersession trail intact.
- The proof car waits honestly (`waits_no_dated_generation` — C190/X290 aren't linked generations yet; the ADR's predicted overlap flag arrives with them). All eight passes converge to exact no-ops; 158 tests green; charter Schema Overview amended; autoevolution evaluated and parked in the ADR (robots.txt AI-crawler wall; terms are social-layer only on a human read — permission ask is the route back). Comment-style discipline promoted to a standing global rule (density tracks difficulty).
- Awaiting review: the PR.

### 2026-08-03 → 08-05 (the cleanup arc: review → front door → comments → plumbing)

- **Three-lens codebase review** (data scientist / analyst / prospective user, 2026-08-03) set the plan: the README described a project four ADRs behind the code ("reconciler: not built" with six passes live); comment density was uniform enough to read as generated even where it wasn't; `matching.py` reached into seven engine privates; ~120 lines of pass bookkeeping existed in triplicate. Substantive gaps named for later: `match_decisions` is captured but nothing consumes it (no precision/recall), `confidence_score` has no writer, no analyst-facing query surface.
- **Front door fixed on main** (`47b5bfc`, `cba155a`): README rewritten against the live DB — 35 tables, six passes, 23,523 configurations, the AMG GT as proof car, the fill sequence, test instructions (a clean clone errored on 125 tests with no hint why); charter's Schema Overview stopped contradicting ADR 0014 (it still said periods FK → generations) and gained the six unlisted tables.
- **PR #30 merged** (comment cleanup, 451 lines out / 239 in): density now tracks difficulty — dated incident stories and self-attribution out of source; QID registries and `derivations.py` kept dense deliberately; the `SS3`→`§3` mojibake fixed (15 sites). Notes drafts withdrawn — the background doc is owed by hand, in the author's own words.
- **PR #31 merged** (`refactor/reconciler-plumbing`, `91e9831`): new `bookkeeping.py` (`mark_reconciled` / `DecisionLog` / `trigram_candidates`) replaces four `_mark` copies, three decision-upsert copies, two trigram copies; `_Pass` → `CompaniesPass` with `admit_entity`/`apply_facts`/`resolve_admission_flags`, so the corroboration path uses an API instead of privates; both 150+-line constructors split into named `_load_*` builders; thresholds named; `DecisionLog` sorts on flush (determinism strengthened). **Verification: test tree byte-identical and 145 green; all six passes re-run live as exact no-ops; `status.py` before/after identical to the line; adversarial 12-agent diff review found nothing above nit — its one catch (a dead `_DECISION_CHUNK`) is fixed in the commit.**
- **PR #33 merged** (same branch — the follow-up fix `593651c` landed after #31 had already merged, so it needed its own PR): the cold-read of the refactor caught a real shadowing bug (`_materialize` cached new configurations under the string `'flex_fuel'` instead of their natural key — latent, one code motion from a duplicate insert), two dead `MappedRecord` fields, and two facts the comment cleanup over-cut, now restored (the direct-P31 measurement beside `MAKES_QUERY`; the reconciler version table → `docs/progress-archive/reconciler-versions.md`).
- **PR #32 merged** (`feat/matching-eval`): `scripts/evaluate_matching.py` reads back the labeled set — 94 curated judgments, 66,537 decisions, 45,531 mechanical accepts, 0 recorded contradictions — and says in its own output why that is a lower bound rather than precision (empty negative registry; unworked queues). Names the three gaps for the methodology ADR: no true negatives anywhere, unwinds leave no machine-readable label, overturn definition undecided. Incidental finding: re-runs overwrite the decision log's first-acceptance outcome with `*_refreshed` (method survives per ADR 0013 §4, outcome doesn't) — for the methodology ADR to rule on.
- Owed next: the matcher-evaluation methodology ADR (what counts as an overturn, how negatives accumulate — before any precision number is quoted against the Tier-2/3 gate), the `confidence_score` write-or-drop decision, and a `v_configuration_full` view + example queries for the analyst path. Current Focus / In Flight above still narrate the pre-0015 state in places; they're owed a refresh pass.

### 2026-07-31 (part 6 — ADR 0015: powertrain facts, not entities; the DCT truth-telling correction)

- **The entities question inverted under investigation** (20-agent parallel sweep, every load-bearing claim adversarially re-derived, all held): EPA cannot name an engine — `engId` is a recycled per-(year,make,model) index with a 25% zero sentinel and an 11-year dead zone; `eng_dscr` is 99% attribute flags (no "B58" in 49,995 rows; the Supra/Z4 twins share specs but no key); `trany` names zero gearboxes. Minting would build spec-bins that split real shared engines and merge distinct ones. **ADR 0015 accepted as amended**: facts-only from EPA; engine entities wait for whichever engine-naming source lands first (vPIC's decode layer is the leading candidate, not the assumption); **no `automated_manual` bucket** — the ruling: AM/AM-S rows assert *nothing*, because no word may erase the single-clutch/dual-clutch difference; `sequential` seeded dormant; no gear-count EAV (nothing to tie it to).
- **Implemented same session** (`be5f3f2`, reconciler v12): corrected trany mapping (AM→NULL, AV→cvt), cng/hydrogen fuel rows, aspiration/flex-fuel EAV under migration-registered keys (`ea565b7ea489`), and the pass's new **sole-source column refresh** — where EPA is the only live asserter and the recomputed answer changes, the old assertion is superseded and the column follows; other-source columns untouched. Live: **2,502 columns corrected** (every DCT's false `automatic` now honestly NULL — the GT-R spot-check), **9,761 EAV facts**, run 2 an exact no-op, 145 tests green. Awaiting review: the PR.

### 2026-07-31 (part 5 — PR #28 merged; the source inventory for the next data turn)

- **PR #28 merged**; main synced, both new passes double re-ran as exact no-ops, 140 tests green. Direction set: with real cars in, go get the rest of what vPIC and EPA offer.
- **Source inventory taken** (fill rates measured live; vPIC variable list probed live): EPA vehicles.csv has ~70 unconsumed columns — near-universal tier (annual fuel cost, CO2 g/mi, petroleum barrels, EPA size class ~97-100%), mid tier (engine id/descriptor 63-75%, luggage/passenger volumes ~34%, smog/GHG scores ~36%, start-stop 37%), electrified tail (EV/PHEV range, charge times, motor, alt-fuel doubles ≤4%). EPA also publishes `emissions.csv`, joinable on the same vehicle id our 13,565 external ids already hold. vPIC's landed payloads are deliberately thin identity feedstock; the source's depth is its **144 VIN-decode variables** (Body Class, Doors, Series/Trim as filed, 20 engine fields incl. displacement/kW/turbo, battery pack fields, GVWR, plant country, ~40 safety-equipment fields) — reachable per VIN pattern or via the monthly full-database download, which needs its own probe-first design turn (the WMI/manufacturer sweep scoping note stands).
- Next: pick the EPA long-tail slice (columns vs EAV per the 80% rule) and the vPIC spec-depth path; each an ADR before implementation.

### 2026-07-31 (part 4 — ADR 0014 accepted and implemented: the first whole cars)

- **The fundamentals review became the doctrine** (review verdict: "serving as a goal per car makes this adr 0014 decision even better"): the hierarchy is a goal per car, not a form every car must fill. The single-car trace (2019 Mercedes-AMG AMG GT S Coupe) found the decisive case — vPIC files ONE `AMG GT` whose 2019 model year holds C190 coupes and X290 4-doors simultaneously — so generation placement moved from the period to the **configuration** (nullable, evidence-gated). Charter amended; ADR 0009 carries the revision pointer; chassis codes confirmed not-a-level (per-make grain; pages over codes are views).
- **Migration `c2d02b91b922`** (hand-written, empty-table guard, refuse-don't-discard downgrade): periods swap `generation_id` for `model_id`; configurations gain nullable `generation_id`. **Migration `de1fcf30fd16`**: the dimension rows the passes consume (US market, drivetrains, fuel/transmission types) are now migration-seeded — they existed in dev only as retired-demo-seed leftovers, which the test DB exposed on first use.
- **The vPIC year pass**: 18,339 periods from 2,018 year lists (280 wait under unmatched makes); run 2 an exact no-op; the 8 run-1 "existing" hits are the Santana double-MakeId overlap behaving correctly.
- **The EPA attach pass** (reconciler v11): 42,920/49,995 rows attached — exact 13,887 / baseModel 25,150 / word-boundary trim-parse 3,883 (the same-day Ranger/Range Rover lesson baked in) — into **23,523 configurations** (groups merge; columns only on unanimity, 1:1 external ids only, provenance per written field). Residue: 495 unbridged-make + 6,580 no-model-match rows behind 802 distinct-string flags; `EPA_MAKE_MATCHES` starts empty by design. 412 periods minted beyond vPIC's lists. Both runs converge exactly; all six passes no-op; **140 tests green**.
- **The proof car exists**: `mercedes-benz-amg-gt-2019-s-coupe-rwd` — 4.0L V8, 18 mpg combined, its own EPA id, generation honestly NULL — beside all 9 of its 2019 siblings. Engines/transmissions and the generation-placement pass are the next design turns, each its own ADR.

### 2026-07-31 (part 3 — PR #26 merged; the JLR wave verified; scripts split by species)

- **PR #26 merged**; main synced, wd-models pass double re-ran settled (identical counts, 0 assertions, 0 flags), 129 tests green. Session-start diff: every handoff number verified against the live DB (477/242/151; methods 439+20+18; queue 618 = 17+52+536+13); the one drift was this file's own head sections, fixed above.
- **The Range Rover verification closed the ruling cross-source**, and the sweep found the whole wave: Discovery (Q140645228) and Defender (Q140645257) are exact siblings of Range Rover's Q140685136 — one Wikidata editing wave, lone `car brand` class, "British car brand owned by JLR", no inception/country, zero catalogue, role-from-class only; vPIC/EPA file all three as Land Rover models. All three ruled not-makes → three `IDENTITY_MERGES` entries → Q35907 staged (application state in In Flight). The 79-name collision sweep otherwise: honest namesakes (Nova/Electra/Century-Buick species), latent-only guard risk (labels carry marque prefixes; Range Rover fired because its labels appear bare).
- **Sub-brand rulings recorded**: Century stays held (real 2025 Toyota spin-off — a marque question, not an artifact); Scion stays parked for the additional-information plan; Ioniq stays held with an empty catalogue (failed division, cars squarely under Hyundai as filed).
- **`scripts/` split by species** (`8b257cb`, from the sprawl concern raised in review; repo stays public — the scripts are the portfolio evidence): `scripts/pipeline/` holds the re-runnable ingest/reconcile shims (future Dagster assets), `scripts/decisions/` the applied-judgment scripts (the executable half of their ADRs); `status.py`/`backup.sh` stay top-level where docs point. No READMEs, as ruled — the scripts' own docstrings carry the explanation. Dagster timing refined (see Phase 1 horizon): manual conducting until continuously-updating sources arrive.
- **The merges applied (collapse run by hand; the dry-run-first gate is standing procedure), and the converge run caught two guard bugs** (`a914a1d`): (1) `normalize_name` strips spacing, so the cross-badge guard's raw `startswith` read the held brand **'Ranger' out of "Range Rover (1st generation)"** — the false alarm survived its own merge by hopping brands; brand prefixes now end on word boundaries of the label. (2) The open-flag refresh fired only on a changed *reason*, so the same-reason answer flip (cross-badge True → False) kept stale detail; the whole detail now refreshes when the computation differs — the flag really is the current question. Both fixes regression-tested (131 green); wd-models pass settled identically twice with zero assertion churn; flag 4567 now reads `cross_badge: false` and the census is 9 cross-badges, all real (Solterra, Trailseeker, Getaway, Raize, GR86, FR-S, LX, Charade, Altis). Family-prefix carve-out has the mirror substring edge (Ford/Fordson species) — errs toward *not* flagging, left as-is, noted here.

### 2026-07-31 (part 2 — ADR 0013: labels over aliases, refined by the attached-match audit; PR #26)

- **The switch approved with a caution** ("go over this as a data engineer/scientist/user — I don't want cars miscategorized"), and the pre-implementation audit of the 387 attached matches vindicated the caution by *reshaping the rule*: 37 "alias-only" attachments were 17 stripping artifacts ("Audi AG" never stripped "Audi A3"), ~15 correct US-market-name matches (Echo, LeCar, Sentra — the alias IS the as-filed name), and exactly **one live miscategorization** (Subaru Trailseeker on `toyota/bz-woodland`). A blanket alias ban would have traded ~15 true matches for 1 true negative.
- **ADR 0013 (accepted, implemented, PR #26), reconciler v10**: prefix stripping uses every recorded name a company wears including its **vPIC make names**; rung-3 claims rank label > alias; one label claimant attaches, alias claimants never cluster (uncontested same-brand attach with method logged; contested or **cross-badge** — label wearing another held brand — flag `market_name_or_rebadge`); refreshes preserve and backfill the decision-log method. Open flags now refresh their reason when the question changes between runs; closes stay immutable.
- **Applied live** (`scripts/apply_adr_0013_refresh.py`: Trailseeker unwound; lines/generations rebuilt under corrected stripping before the year pass gives them consumers): **477 models matched 1:1** (+90 recovered from the contaminated clusters), 242 lines, 151 generations; queue recomposed to **17 label-twin clusters + 52 market/rebadge (10 cross-badge: Raize, FR-S, GR86, Solterra...) + 536 near-miss + 13 ambiguous/other**; methods fully recorded (439 prefix_stripped_label / 20 exact_label / 18 alias-carried = the standing audit query). Consecutive runs identical, zero supersessions; older passes no-op; 129 tests green. Companion page updated to the four-bucket queue.
- Honest caveat recorded: a run that changes the world (new attachments) can re-route a handful of dependent entities on the *next* run — fixed point by the second re-run, churn-free throughout (only flags/log outcomes move between settling runs, never assertions).
- **Range Rover ruled not a brand** (session close): it's a Land Rover model line — the filings agree (vPIC Make=LAND ROVER, Model="Range Rover Sport") — so the held "Range Rover" company row is the brand-artifact disease; next session verifies cross-source, then proposes the IDENTITY_MERGES entry (→ Land Rover), which also dissolves the one cross-badge false alarm. Session ends with a handoff prompt to a fresh session (context budget); PR #26 awaits review.

### 2026-07-31 (PR #25 merged; the review's three questions answered with a live-data companion page)

- **PR #25 merged** (the whole pass reviewed). The review questions — why chains can't create, what the label-twin trap left, what the 658-flag queue is — answered on a companion page built from the live rows (three real chains: X5 E53→E70→F15 siblings, Ford Model N→T→A successors, BMW 2002→E21 boundary-cross — identical edges; the 47-cluster table; queue = 47 clusters + 593 near-miss + 18 ambiguous, with the 2 similarity-1.0 near-misses identified as deferred-claimant echoes of open clusters). Vocabulary note recorded: "rung" (ADR 0008's ladder) read poorly in review; "level" collides with the hierarchy — "step" is the plain candidate if `match_decisions.rung` gets renamed.
- Housekeeping: `.claude/`/`CLAUDE.md` confirmed ignored via the global excludes file, `.github` is tracked (its earlier graying was the anomaly) — the VS Code decoration change is cosmetic. Postgres container restarted after overnight host shutdown; state intact. Next: the wd-model flag queues, then the year-pass/EPA ADR (go pending).
- **Badge engineering found in the clusters** (queue-working session: Raize∈rocky, Urban Cruiser∈scion-xd are rebadges/market twins, not label twins — "different cars", the VW-group framing; rung→step rename declined as not worth it; chain edges confirmed needing another source). Mechanism confirmed live: Wikidata records rebadge/market names as **aliases** (Q85872511 label "Toyota Raize", P176 Daihatsu, aliases "Daihatsu Rocky|Perodua Ativa|Subaru Rex"), so every contaminating claimant hit via alias, never label. Triage: **14/47 clusters are pure label twins; 33 are alias-contaminated** (Frontier/Navara, 4Runner/Hilux Surf, Highlander/Kluger, BRZ/FR-S/GR86, Land Cruiser/Lexus LX...). **Proposed, awaiting a go (matcher policy)**: label hits outrank alias hits — sole label-claimant attaches 1:1; alias-only claimants flag separately as `market_name_or_rebadge` (cross-badge → negative pairs + future `vehicle_derivations` rebadged facts, ADR 0005's table; same-brand market names → the ADR 0011 §5 model-registry question). Empties ~33 clusters mechanically. Decision-log wart noted: re-runs upsert rung-1 `external_id` over the original match method — preserve first-match method on refresh (small fix, fold into the same change). Triage table on the companion page.

### 2026-07-30 (part 8 — ADR 0012 implemented: the sweep lands, the first generations exist, and the label-twin trap fires live)

- **The sweep** (probed live before building): population 14,524 — the ADR's three classes exactly; `ORDER BY ?item` costs ~90s server-side so the QID list fetches unordered (0.5s) and sorts client-side; details come in 49 `VALUES`-batches of 300 (2.1s each), commit per batch. Bare QIDs + the stamped `sweep: models` marker; **30 QIDs landed from BOTH sweeps**, so `_current_records` now partitions on the marker — without it whichever sweep landed last would shadow the other pass's view.
- **The pass** (reconciler v9): rung-3 matches are **claims resolved per model** — the first live run attached 504 models but 51 of them had 2–4 claimant QIDs each (generation entities carrying the bare nameplate label: four "BMW X5"s with E53/E70/F85 sitelinks — the four-3-Series lesson at nameplate level) and their summary assertions superseded in a cycle (96/re-run). Now one claimant attaches 1:1; several → one flag carries the cluster with descriptions + sitelink titles; a claimant whose P179 points at another claimant defers and becomes that model's generation. The buggy first run's derived state was reverted (dev-only, rebuilt from raw) and the fixed run re-ran as a true no-op.
- **One documented interpretation over the ADR**: chain edges (P155/P156) are ordering, never creation evidence — Model T *follows* Model S (two nameplates) and E21 *follows* the 02 Series are structurally identical to sibling generations, exactly the guess "never fuzzy" forbids. Chain-only evidence waits, logged; the year-pass ADR gets the chains with vPIC's time axis.
- Live: 387 models enriched, 251 lines, 7 memberships, 105 generations, 463 line-case waits, 658 wd-model flags. Labeled set capturing from run one (`match_decisions` 14,524 rows; every flag close carries a resolution; `WIKIDATA_MODEL_NEGATIVES` consulted before any accept). `models_pass.py` renamed `vpic_models_pass.py` (ruled mid-session: name the first models pass by its source). 124 tests green.
- **Warts logged for existing open questions**: prefix-stripping uses `companies.name`, so "Audi AG" strips nothing from "Audi Q3" — line/generation display names keep the marque prefix for exactly the corporate-name makes the EPA bridge finding already named (display-name/slug ADR). The Kia K5 line landed under the Hyundai Motor Company page via its P176 — source-asserted, review-worthy, in the queue.

### 2026-07-30 (part 7 — the backfill lands whole; the approved merge batch applies; session handoff)

- **Model-years backfill complete**: 11,609 requests, 2,018 records, **100% coverage** (every passenger model has a 1981+ year list; zero missing), 18,206 non-passenger rows skipped by the passenger-set filter. Validation: Accord = unbroken 1981–2027; M3's gaps (1992–94, 2007, 2014, 2019–20) are its real production gaps; 330i shows the E46/G20 two-era shape. Annual top-up from here ≈ 247 requests.
- **The duplicate-sweep review** (105 exact-name groups, 230 rows) ran as classified proposal → **the merge bucket only** approved; namesake and ambiguous groups wait for cross-source checks (ruling: "need to be checked against other sources before they are brought in"). Applied: 11 same-entity pairs into `IDENTITY_MERGES` (v8), collapsed live (companies 7,215 → 7,204), all three passes re-run as no-ops, 106 tests green. Noted: Techrules' canonical keeps a QID-suffixed slug (slug-ADR wart); TVR's twin is an assembly plant needing a deny, not a merge.
- Session ends with a handoff prompt to a fresh session (context budget): implement ADR 0012 next.

### 2026-07-30 (part 6 — the direction review: two independent lenses, one live bug, verdict sound)

- **Requested direction review** run as two independent reviewers (data-scientist and data-analyst briefs) over the database + recent merges. **Both verdicts: right direction** — the 1:1 external-id rule, never-fuzzy ladder, provenance lineage (0 of 28,620 live assertions missing raw pointers), fetch-wide/write-narrow, and the tabled expansion all named as the load-bearing strengths.
- **One live bug found and fixed same-hour** (`7595794`): the models pass still selected records by payload shape — the lesson learned twice — and the in-flight `modelyears:` records (carrying `model_id`+`make_id`) would have opened ~1,500 spurious collision flags on the next re-run. Kind-prefix selector + regression test; zero flags actually fired.
- **The two structural findings**: (1) **the labeled set isn't being captured** — rung-2 auto-matches record no method, 61/64 dismissed match flags carry no resolution, negative judgments live only in policy comments; the charter's Tier-2/3 gate ("matcher precision measured") is uncomputable as-is → match-decision log + resolution discipline + negative registry fold into the ADR 0012 implementation, before its 14.6k-entity pass. (2) **the EPA attach bridge is quantified and needs design**: exact model-string joins hit 28% of EPA rows (BMW: 0 of 480 — EPA strings are trim-grained; its `baseModel` is line-level), and the make bridge must route via vPIC make names (corporate names like "Audi AG" drop ~3,300 rows) → acceptance metrics into the year-pass/EPA ADR.
- **Scion run down to a better answer than the review's**: not a coverage hole — vPIC has no Scion make (VIN layer: Make=TOYOTA, Model="Scion tC") and all 8 Scion nameplates are landed under Toyota as-filed. It becomes an EPA-bridge registry entry + a marque-presentation question (Scion company row with 0 models, same species as Land Rover Santana).
- Queued from the review: the 105-name duplicate-company sweep (while losers are childless — compounding cost), status.py observability lines (reconciled-coverage per kind, flag ages, labeled-set counts), incremental/batch-committed passes before EPA-scale writes, model-level canonical-record rule + merge registry (the Santana name-churn edge), display-name question → slug ADR, summary language pass → pre-pages, admission-queue burn-down realism (94% boilerplate-only; list-editing leverage exhausted) → parked with barely-cars.

### 2026-07-30 (part 5 — ADR 0011 accepted; EPA lands; the year fetch is built and armed)

- **ADR 0011 accepted** (PR #22) — with a merge-race footnote: the merge and the Santana push crossed, stranding commit `b45f91d` on the merged branch; recovered by cherry-pick onto main. Lesson: once a PR is under review, no further pushes without checking merge state first.
- **The two safe landings built** (`feat/vpic-years-epa-landing`): vPIC model-years fetcher (`modelyears:<id>` per-model sorted year lists, untyped endpoint intersected against known passenger ModelIds, 1981→current+1 policy bound, commit-per-make so the long sweep is resumable) and the EPA bulk lander (`vehicle:<id>` per CSV row, untransformed). **EPA ran live: 49,995 rows, 1984–2027, re-land 0 new.** The vPIC sweep (~11.6k requests ≈ 3.2 h at the polite rate, one-time; annual top-ups ≈ 4 min) awaits a go. Fetch-wide note: the year endpoint itself carries nothing beyond the (model, year) link, so "grab more while there" means chaining other sweeps — manufacturers/WMI needs its own scoping probe (different id space; naive sweep ≈ 17k requests).

### 2026-07-30 (part 4 — Santana resolved: one page per wheelbase; the model queue hits zero)

- **The 3 slug-collision flags resolved as merges** (ruled: "a diff page for each wheelbase makes sense" — three pages, not six): each LAND ROVER SANTANA ModelId attached to the row its SANTANA twin created (`scripts/apply_santana_model_merges.py`, idempotent), flags resolved as `same_nameplate_second_makeid` — resolved with the verdict recorded, never deleted; they are the first model-level labeled decisions. Models pass now reconciles **1,738 records onto 1,735 rows**, zero open model flags. Sharp edge logged for the ladder ADR: a multi-id model's twins currently both assert `name` (harmless while byte-identical; a one-sided vPIC rename would need the company engine's canonical-record rule).
- "Barely cars" batch-triage **delayed until much later** (ruled: "after we figure out what cars are"); the no-match pool parks with it. Awaiting review shrinks to PR #22 alone.

### 2026-07-30 (part 3 — no artifacts left: the demo raw records go too; C-Class answered by VIN decode)

- **ADR 0011 §3 amended per review** ("I don't want any artifacts from the 3 series seed left over"): the three fabricated `/demo` raw records deleted under ADR 0004's own-artifacts clause (fabrications are not evidence), with their false-attribution rows — 3 superseded company-arc history rows, the seed's raw-less Wikidata role row on BMW, 1 reconciled_records row. Companies pass re-run re-asserted the role from the real Q26678 record; **zero raw-less provenance/role rows remain database-wide**, and Wikidata raw = 9,889 = the pass's processed count (the demo record used to break that cross-check). BMW row and Q26678 mapping stay: substantively real, adopted by upsert-by-natural-key — the artifact test is false attribution or fabricated content, not who inserted first.
- **C300-vs-C-Class answered with evidence** (the carve-out question): vPIC's VIN layer decodes a W205 pattern as `Model=C-Class, Trim=C300` (and BMW's `Model=X3, Trim=xDrive35i`) — the badge lands in `configurations.trim_name`, one level below Model, for every make; only where the manufacturer draws the model/trim boundary differs. Probe recorded in ADR 0011 §1. Configurations at zero are temporary: year-level vPIC + EPA per-variant rows are the feedstock, blocked only on the generation question.
- Santana added to the company-eras Open Question (licensee era → defunct 2011 → EV revival); the 3 collision-flag merges still await a ruling.

### 2026-07-30 (part 2 — ADR 0011: series are lines, not entities; the demo seed retires)

- **Second ruling on the model level**: series/lines ("3 Series", the class-*concept* behind C-Class) should not be stored as entities — "it's more a line", useful for browsing BMW's history but not a model. Drafted **ADR 0011** (as-filed leaf designations; series are aggregations served by a future membership relation + view; our identity is our PKs and external ids are 1:1 correspondence only, which dissolves the QID fan-out wrinkle; source-side duplicates resolve by curated merges, not constraints — our uniques police our rows, they cannot deduplicate Wikidata's four "BMW 3 Series" entities). One interpretive line flagged for review: **C-Class stays** — it is Mercedes' actual filing, not an aggregation we created; removing it would orphan the make and the next pass would recreate it from raw.
- **Demo seed retired live** (`scripts/retire_demo_seed.py`, idempotent; `seed_demo.py` deleted, README updated): the 3-series → E46 → 2002 → 330i-us-sedan chain, demo engine/transmission, 14 provenance rows, 4 external ids. Raw records and reference data stay. Now models=1,735 (pure vPIC), generations/periods/configurations=0 — every entity row is reconciler-produced. Both passes re-run as no-ops; 99 tests green.
- Generation-source plan de-Wikidata'd per review: Wikipedia's per-generation infoboxes (Tier 2) evaluated beside Wikidata in the fetch ADR; Wikidata stays first for cheap structure. Santana context recorded (VW Santana is a model, not the make; the wheelbase models under both MakeIds corroborate the Spanish licensee pin); merge verdict still awaited.

### 2026-07-30 (the ModelId ruling: models are what the manufacturer files)

- **The granularity question settled in review**: vPIC models stay as-filed — the leaf version of the car, one page each ("I want a 308" → "GTB or GTS?"); naming style is the company's own decision and the schema absorbs it (generations carry time, configurations carry engine/body/trim — the 4Runner shows several engines, the 328d exactly one). Sharpened against live data: Mercedes files *classes* (C-Class) where BMW files badges and Porsche files one `911` across six decades; the placeholder-looking names are mostly real nameplates (Toyota `Van`, Maserati `Coupe`, Audi `Coupe`). Recorded in Open Questions; ADR owed with the year pass.
- **Generation source probed live**: Wikidata holds generation entities (BMW E30) but P179 is sparse, series entities are duplicated (E30's series ≠ Q194352), and 4Runner shows none — sweep design must discover multi-property and expect gaps; the QID fan-out question (one E46 entity vs per-model generation rows) is logged for the ADR. The probe's late-returning half sharpened it: E30's neighbours chain by P155/P156 only, four distinct entities share the bare label "BMW 3 Series", and P31 is `car model` for generation and nameplate alike — **level is structural, never nominal**. (WDQS also 504/429'd mid-probe; the polite client's backoff handled it, but the future sweep should expect flaky service.) Fetch posture ruled: **fetch wide, write narrow** — mine each source maximally into raw; entity-writing stays ADR-gated, and the narrow-pass sequence so far was scaffolding, not doctrine. F2 pages parked (database fill first). Postgres container restarted after a clean host shutdown; state intact.

### 2026-07-29 (part 11 — ADR 0010 accepted and implemented: 1,735 models, and a latent match-pass bug caught)

- **ADR 0010 accepted as written** and implemented as `carmanac/reconcile/models_pass.py` — a sibling pass, not an engine generalization: the engine's `_Pass` is company-shaped throughout, so reusing its `slugify`/`_current_records`/`_supersede` (as `matching.py` already does) beat parameterizing seven arc columns for one field. **Live: 1,735 `models` rows** under 129 companies, 1,735 `name` assertions under the model arc, 280 records skipped under unmatched makes, **3 slug collisions flagged** — all Santana, where `SANTANA` (13765) and `LAND ROVER SANTANA` (13766) are both pinned to Q265465 and deliver `110" WB` / `90" WB` / `88” WB` twice each. Predicted before the run by replaying the ADR's slug rule over the landing zone, so the live numbers were a confirmation, not a discovery. Re-run: 0 created, 1,735 matched, 0 assertions, 0 flags, every count identical. Both older passes still no-op. 8 new tests; **99 green**.
- **A real bug on main, caught by the new tests before it fired**: the match pass selected its records by payload shape (`make_id` + `make_name`), and a *model* payload carries both — deliberately, since that link is how a nameplate finds its make. The next match-pass run would have read all 2,018 model records as makes and attached `model:<id>` external ids to **companies**. Live rows were untouched (the models landed after the last match run), so the fix is a one-line selector change to the kind prefix — the thing the 2026-07-29 re-key bought — plus a regression test and the same fix in `landed_make_ids`, where the defect was harmless only by accident. **Lesson: record kinds are told apart by their namespaced id, never by payload shape, because kinds legitimately share fields.**
- **The live rows raise the year pass's real questions** (both now Open Questions, both blocking): vPIC's model granularity is inconsistent by make (Toyota gives nameplates, BMW gives `318i`/`318ti` trims, Ferrari splits by body), and **neither vPIC nor EPA asserts a generation at all** — so there is nothing legal for a `model_year` period to hang under. Fabricating a generation per model is the provenance smell ADR 0009 already rejected one dimension over. This puts the **Wikidata models fetch on the critical path**: it is the nameplate-and-generation source both questions want.
- `scripts/status.py` gained a models-reconciled line and now splits `match_review` by record kind — the make queue (113, parked) and the model queue (3, collisions) are different questions and one number hid whichever grew.

### 2026-07-29 (part 10 — PROGRESS restructured: hot head + archive + status.py)

- This file split per the growth concern raised in review (PR #19): head keeps intent (focus/in-flight/next/open questions/ADR index/recent log, ~110 lines), history rolls to `docs/progress-archive/` verbatim, and `scripts/status.py` prints the live numbers so session-start verification is a diff, not an archaeology dig. PROGRESS **stays tracked**: git is the multi-device sync, and the log is portfolio evidence like the ADRs.

### 2026-07-29 (part 9 — the first cars land: 2,018 vPIC passenger models)

- **vPIC models fetch-and-land built and run** (`carmanac/ingest/vpic/models.py` + script, same plumbing as makes): `GetModelsForMakeIdYear/makeId/<id>/vehicletype/{car,mpv}` per landed make, union per ModelId (the Accord appears under both types), sorted type lists, `model:<ModelId>` external ids (the re-key paying off on day one). **2,192 model/type rows across all 247 makes → 2,018 distinct models landed**; ~9 minutes at the polite 1 req/s. Endpoint shape probed live before building (models arrive mixed-case — "Accord", "FCX Clarity" — unlike the SHOUTING makes). 3 landing tests; 90 total green.
- **All 247 makes fetched, not just the 134 matched** — landing is generous (Tier 1 re-fetchable cache) and models under unmatched makes are match-queue evidence. Immediate payoff: **BBC's only "model" is literally named "Passenger Car"**, a vPIC placeholder — evidence *against* it being the Italian BBC brand; the flag stays open but better informed. Sanity: BMW 146 models, Ford 140, Ferrari 66; Accord/Viper/Roadster resolve by ModelId.
- **ADR 0010 proposed** (the models pass): matched-make models only (unmatched makes' models wait — one open make question must not fan out into fifty model-shaped copies); upsert by identity ladder then (company, slug); slug collisions under one company FLAG rather than auto-suffix (two same-slug models under one make are usually the same nameplate — auto-suffixing would mint exactly the duplicate-identity problem the merges just cleaned); `models.name` projects via `field_provenance` so Wikidata can arbitrate later; nameplate level only. Needs review before implementation.

### 2026-07-29 (part 8 — requested migration review; one real find)

- **Deep review of the hand-written migration** (brief: "everything transferred right and functionality remains"). Full catalog sweep for `model_year` across pg_class/pg_constraint/pg_trigger/pg_attribute/pg_indexes/pg_sequences/pg_proc **found 8 stale names**: a column rename rewrites index *definitions* but not the attribute names stored inside existing index relations — `uq_configurations_natural_key`, `uq_field_provenance_live`, `uq_media_attachments_asset_entity_role`, and the five renamed arc/FK indexes all still said `model_year_id` in pg_attribute. Functionally inert (planner and pg_dump use definitions — verified none carried the old name), but the catalog shouldn't lie. Index relations accept `ALTER TABLE ... RENAME COLUMN`; migration amended (both directions), live DB brought to the amended end-state, downgrade/upgrade round-tripped through the amended code.
- **Everything else verified clean**: catalog sweep now empty; partial arc-index predicates carry the new column name; sequence owned by `catalogue_periods.id`; trigger semantics re-proven across separate transactions (first probe was flawed — `now()` is transaction-fixed, so same-transaction INSERT+UPDATE can never show a bump: no-op update doesn't bump, real update does); five-level seed join resolves (`bmw > 3-series > e46 > 2002-2002 (model_year) > 330i-us-sedan`); table counts intact (7,215 companies / 10,139 raw / 26,902 assertions / 2,850 flags); both reconciler passes re-run as no-ops; seed idempotent; 87 tests green. Code sweep: only the kind code and the already-stale-flagged legacy docs mention model years.

### 2026-07-29 (part 7 — the catalogue_periods migration; ADR 0009 implemented)

- **Migration `76cb287dd71c`** (PR #17), hand-written — a table rename is autogenerate's DROP+CREATE blind spot, and the trigger is invisible to it (both documented since ADR 0006's migration). `model_years` → `catalogue_periods` with every embedded name following (PK, indexes, FK-constraint names, trigger, **and the id sequence** — the ADR 0006 rename left `makes_id_seq` behind on `companies`, noted, not repeated); `year` → (`start_year`, `end_year`, `period_kind_id`) with the seed row backfilled as a model_year period; seeded `period_kinds` lookup; four-column natural key NULLS NOT DISTINCT; CHECK end ≥ start. The five referencing tables' `model_year_id` arc/FK columns renamed to `catalogue_period_id` — column renames rewrite constraint/index *expressions* automatically (verified live on the exclusive-arc CHECKs); only *names* need explicit renames.
- **Downgrade refuses instead of flattening**: real period/phase rows cannot round-trip into a single `year`, so `downgrade()` counts non-model_year rows and raises (the same refuse-don't-discard posture as f3c645b9cb6f's). Round trip verified on the pre-period dataset.
- Verified: `alembic check` clean, downgrade/upgrade round trip, pre-migration seed data survived the rename intact, seed script idempotent on the new shape, arc CHECK expressions rewritten, trigger fires on the renamed table. **87 tests green** (3 new: open-ended-period NULLS NOT DISTINCT collision, end-before-start CHECK, both kinds coexisting on one generation — the ADR's mixed-granularity claim as a permanent test).
- Test-fixture lesson re-learned from conftest's own docstring: migration-seeded lookups (now incl. `period_kinds`) are deliberately NOT truncated between tests — fixtures select them like production code does, never re-insert.
- **Configuration-level ingestion is now unblocked** (year-level vPIC, EPA). Next: vPIC models fetch-and-land.

